# This project intentionally avoids third-party dependencies so the pipeline
# can run in offline environments. If a future change introduces a library
# requirement, add it below using the standard ``package==version`` format.